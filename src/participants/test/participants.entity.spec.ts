import { ParticipantsEntity } from '../entity/participants.entity';

describe('ParticipantsEntity', () => {
  it('should be defined', () => {
    expect(new ParticipantsEntity()).toBeDefined();
  });
});
